## What this changes

A short description of the change and why it's needed.

## Type

- [ ] Feature
- [ ] Fix
- [ ] Design-system change
- [ ] Docs

## Checklist

- [ ] `npm run build` passes (production build + type check + lint)
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run lint` passes with zero warnings
- [ ] Motion/UI changes reuse existing variants from `lib/motion.ts` where possible
- [ ] Screenshots attached for any visual change

## Screenshots

Before / after, if this touches UI.
